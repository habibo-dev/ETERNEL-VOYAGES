import { NextResponse } from 'next/server'
export async function POST(req:Request){try{const body=await req.json();if(!body.name||!body.phone)return NextResponse.json({ok:false,error:'name and phone are required'},{status:400});console.info('Eternel Voyages lead',body);return NextResponse.json({ok:true,message:'Lead received'})}catch{return NextResponse.json({ok:false,error:'Invalid request'},{status:400})}}
